namespace PackingCygest.Model
{
    /// <summary>
    /// Enum For Ms Model, to ensure only the two below values are allowed
    /// </summary>
    public enum MSModel
    {
        UMS,
        KMS
    }
}
